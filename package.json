{
  "dependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.5.0",
    "vite": "^6.3.5",
    "zustand": "^5.0.5"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  }
}
