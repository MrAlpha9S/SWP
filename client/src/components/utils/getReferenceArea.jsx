import React from 'react';

const GetReferenceArea = (dataset) => {

    if (!dataset || dataset.length === 0) return null



    return (
        <div>
            
        </div>
    );
};

export default GetReferenceArea;